# Node Legacy Archive

This directory contains the retired Node/Fastify backend, the old shared TS contract package, and architecture notes from the pre-Rust mainline.

It is kept only for historical reference and performance comparison. It is not part of the default runtime, CI, or packaging flow.
